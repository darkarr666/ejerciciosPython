En resumen: estructuras fundamentales de Python

Categoría	            Ejemplos comunes

Sentencias	            if, for, break, def
Funciones	            print(), sum()
Métodos	                .append(), .upper()
Operadores	            +, ==, in, and
Clases/Objetos	        class, objeto.metodo()
Expresiones lambda	    lambda x: x+1
Comprehensions	        [x for x in ...]
Decoradores	            @decorador

GIT:

Antes de trabajar => git pull

Al terminar =>  git add .
                git commit -m "Comentario"
                git push

Función main:

def main():
    print("Este código se ejecuta solo si se ejecuta el script directamente.")
    # Aquí va la lógica principal del programa
    nombre = input("Ingresa tu nombre: ")
    print(f"Hola, {nombre}!")

if __name__ == "__main__":
    main()